apiVersion: apps/v1
kind: Deployment
metadata:
  name: helloword-app
spec:
  selector:
    matchLabels:
      app: helloworld-app
  replicas: 2
  template:
    metadata:
      labels:
        app: hellowrld-app
    spec:
      containers:
      - name: helloworld-app
        image: nanajanashia/argocd-app:1.2
        ports:
        - containerPort: 8080
